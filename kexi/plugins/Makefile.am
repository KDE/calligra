if compile_kexi_reports_plugin
  REPORTS=reports
endif

if compile_kexi_macros_plugin
  MACRODIR=macros
endif

if compile_kross
  SCRIPTINGDIR=scripting
endif

SUBDIRS = tables relations migration queries forms $(REPORTS) $(MACRODIR) $(SCRIPTINGDIR) importexport

#importwizard relations

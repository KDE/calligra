Current todo list:
(add ideas here :-)

- menu: view->zoom in, zoom out, zoom factor
- ruler context menu to select metrics
- paint on/move linked layers
- color buttons (fore-/background) + color dialog
- extend brushtool to use foreground color
- implement pen tool (paint a line using the foreground color)
- implement selections
- extend layerdialog
- undo/redo
- replace BIGNUM with BIG_TILE_MULT
- disallow negative coordinates in channels - move the layer wrt the
     canvas instead
- check that the imageRect is correctly updated when a new point above or
     to the left of the layer


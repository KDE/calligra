/* config.h.  Generated by cmake from config.h.cmake */

/**** WARNING WARNING WARNING :
 Don't even think of adding something here. Define your own config-foo.h.cmake (here, or near the code that needs it),
 and add a configure_file line in CMakeLists.txt in the same directory.
*/

/* The size of a `char *', as computed by sizeof. */
#define SIZEOF_CHAR_P ${SIZEOF_CHAR_P}

/* The size of a `int', as computed by sizeof. */
#define SIZEOF_INT ${SIZEOF_INT}

/* The size of a `long', as computed by sizeof. */
#define SIZEOF_LONG ${SIZEOF_LONG}

/* The size of a `short', as computed by sizeof. */
#define SIZEOF_SHORT ${SIZEOF_SHORT}

/* Number of bits in a file offset, on hosts where this is settable. */
#define _FILE_OFFSET_BITS 64

/* Define to 1 to make fseeko visible on some hosts (e.g. glibc 2.2). */
/* #undef _LARGEFILE_SOURCE */

/* Define for large files, on AIX-style hosts. */
/* #undef _LARGE_FILES */

/**** WARNING WARNING WARNING :
 Don't even think of adding something here. Define your own config-foo.h.cmake (here, or near the code that needs it),
 and add a configure_file line in CMakeLists.txt in the same directory.
*/


#ifndef CALLIGRALIBS_KoColorConversions_H
#define CALLIGRALIBS_KoColorConversions_H

void rgb_to_hsv(int R, int G, int B, int *H, int *S, int *V) { Q_ASSERT(false); }
void hsv_to_rgb(int H, int S, int V, int *R, int *G, int *B) { Q_ASSERT(false); }
void RGBToHSV(float r, float g, float b, float *h, float *s, float *v) { Q_ASSERT(false); }
void HSVToRGB(float h, float s, float v, float *r, float *g, float *b) { Q_ASSERT(false); }
void RGBToHSL(float r, float g, float b, float *h, float *s, float *l) { Q_ASSERT(false); }
void HSLToRGB(float h, float sl, float l, float *r, float *g, float *b) { Q_ASSERT(false); }
void rgb_to_hls(quint8 r, quint8 g, quint8 b, float * h, float * l, float * s) { Q_ASSERT(false); }
float hue_value(float n1, float n2, float hue) { Q_ASSERT(false); }
void hls_to_rgb(float h, float l, float s, quint8 * r, quint8 * g, quint8 * b) { Q_ASSERT(false); }
void rgb_to_hls(quint8 r, quint8 g, quint8 b, int * h, int * l, int * s) { Q_ASSERT(false); }
void hls_to_rgb(int h, int l, int s, quint8 * r, quint8 * g, quint8 * b) { Q_ASSERT(false); }

#endif

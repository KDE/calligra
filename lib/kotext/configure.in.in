   AC_LANG_SAVE
   AC_LANG_C
   dnl Check for aspell library
   KDE_CHECK_LIB(broker, openBroker, [LIBKSPELL2="-lkspell2"])
   if test -n "$LIBKSPELL2"; then
           dnl the header and the lib exist -> ok
           dnl this is for config.h
           AC_DEFINE(HAVE_LIBKSPELL2, 1, [If we are going to use libkspell2 for spell-checking])
   fi
   dnl this is for lib/Makefile.am, to know whether to compile kospell
   AC_SUBST(LIBKSPELL2)
   AC_LANG_RESTORE
